source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "webrick"

# Theme (because _config.yml has `theme: minima`)
gem "minima", "~> 2.5"

# All Jekyll plugins you use go here
group :jekyll_plugins do
  gem "jekyll-sitemap"
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  # add/remove lines to match your `_config.yml` plugins list
end